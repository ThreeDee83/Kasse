{
  "name": "owncash",
  "version": "1.0.0",
  "private": true,
  "description": "OwnCash Web-App und interne Android-Kassen-App",
  "scripts": {
    "build:android-web": "node scripts/build-android-web.mjs",
    "android:sync": "npm run build:android-web && cap sync android",
    "android:open": "npm run android:sync && cap open android",
    "android:debug": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run android:sync && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@supabase/supabase-js": "^2.112.4",
    "pdf-lib": "1.17.1",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
